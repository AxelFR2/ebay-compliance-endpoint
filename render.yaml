services:
  - type: web
    name: ebay-compliance-endpoint
    runtime: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    envVars:
      - key: EBAY_DELETION_VERIFICATION_TOKEN
        sync: false
      - key: EBAY_DELETION_ENDPOINT_URL
        sync: false
